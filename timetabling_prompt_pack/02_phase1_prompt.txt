Phase 1 only.

Build the backend foundation for the timetabling assistant.

Scope:
- create the backend architecture
- design the database models
- implement the matching engine
- expose minimal API endpoints
- add tests for scheduling logic
- add seed data

Do not build the frontend yet unless needed for a simple demonstration.

Before coding, show me:
1. exact folder structure
2. schema definitions
3. matching rules
4. implementation order

Then implement Phase 1.

Requirements:
- Backend: FastAPI
- ORM: SQLAlchemy
- Database: SQLite
- Tests: pytest
- Add clear instructions to run locally
- Include at least 3 teachers, 6 students, and realistic recurring sessions in seed data
- Add an endpoint that, given a teacher, student preferences, blocked times, and lesson duration, returns ranked slot suggestions

Important:
- Keep business logic separate from API routes
- Keep recurrence handling simple and weekly for MVP
- Use timezone-aware thinking in design, but keep storage practical for weekly recurring local schedules
- Add tests for overlap, filtering, clashes, and ranking
